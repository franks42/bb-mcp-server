;; nREPL Test Server module configuration
;;
;; Auto-starts a Babashka nREPL server for testing.

{:name "nrepl-test-server"
 :version "0.1.0"
 :description "Auto-starting Babashka nREPL server for testing"

 :requires []

 ;; Entry point
 :entry "nrepl-test-server.core/module"

 ;; Default configuration (can be overridden in system.edn)
 :defaults {:port 7888
            :host "localhost"}}
